- 単体ワード最大5件
- AND/OR組み合わせ検索最大5セット
- 1セット最大3ワード
- ANDをORより優先
- 全角/半角、大文字/小文字、空白差を正規化
- Excel出力に指定キーワード列、組み合わせキーワード列を追加

デモ側にも `キーワード集計` 画面を追加済み。

新バージョンでもこの考え方を継承する。

## 列順変更・フォーマット変更への対応方針

ユーザー要望:

投入データの列順が変わっても、列名や意味が分かれば分析できるようにしたい。

実装方針:

1. まず列名で自動判定する。
2. 列順には依存しない。
3. 列名が違う場合は、同義語候補で探す。
4. それでも見つからない場合は、必要な要素名を日本語で表示し、ユーザーに対応する列名または列番号を指定してもらう。
5. 指定された列が使える場合は分析を続ける。
6. 使えない場合は、理由を添えてエラー表示する。
7. 必須項目が揃っていれば分析を実行し、不足項目に依存する分析だけ「情報不足で分析していない」とコメント表示する。

管理者画面:

- 集計に必要な要素を一覧表示する。
- 対応する項目名または列番号を登録できるようにする。
- 設定は名前またはファイル名で登録できる。
- 最大3件登録できる。
- チェックして設定を切り替えられる。
- 設定はいつでも上書き可能にする。

## PIPELINE検証用の架空データ

場所:

`input_sample_data/pipeline_mock_data`

ファイル:

- `messages_gyouma-agent_pipeline_mock.csv`
- `chat-raw-data_pipeline_mock.csv`
- `【当日】履歴抽出全国_pipeline_mock.CSV`
- `README.md`

生成元:

`scripts/generate_pipeline_mock_data.py`

検証済み内容:

- GYOMA-AI: 80行
- ChatSupport: 80行
- 電話CRM: 80行
- GYOMA-AIの有人エスカレ: 26件
- ChatSupportの電話引継番号あり: 18件
- 電話CRMの電話引継番号あり: 18件
- ChatSupportと電話CRMの引継番号一致: 18件
- ChatSupport未解決文言: 12件

電話CRMの架空データだけ、将来列として `電話引継番号` を追加している。

## 最初の実装スプリント案

### Sprint 1: 実データ読取の土台

目的:

3種類の受付ログを読み込み、共通の内部データへ変換できるようにする。

タスク:

- GYOMA-AI CSV readerを確認・拡張する。
- ChatSupportの新CSV readerを追加する。
- 電話CRMのCSV readerを追加する。
- UTF-8とCP932を自動判定する。
- 列順に依存しない列マッピングを作る。
- エラー時に、足りない要素名を日本語で表示する。

完了基準:

- 実サンプル3種を読める。
- 架空データ3種を読める。
- 件数、日付範囲、主要列の欠損数を表示できる。
- pytest、ruffが通る。

### Sprint 2: PIPELINE集計

目的:

GYOMA-AI、ChatSupport、電話CRMを横断して、受付・完結・エスカレ・未解決を集計する。

タスク:

- GYOMA-AI完結件数
- GYOMA-AIからChatSupportへのエスカレ件数
- ChatSupport直受付件数
- ChatSupport完結件数
- ChatSupport未解決件数
- 電話引継番号あり件数
- 将来の電話CRM正規エスカレ突合

完了基準:

- 架空データで想定件数と一致する。
- 実サンプルでは確定できないものを候補扱いにできる。
- 「確定」「候補」「情報不足」を混ぜずに表示できる。

### Sprint 3: UI反映

目的:

モックで固めたPIPELINE、数値表、改善テーマ、QA候補を実アプリ画面へ反映する。

タスク:

- PIPELINE概要画面
- 左から右に「減った・増えた」が分かる数値表
- カテゴリー別分析グラフ
- QA候補管理
- 品質・企画FB
- キーワード集計

完了基準:

- 架空データを投入するとPIPELINEが分かりやすく表示される。
- 実サンプルを投入しても、未対応・不足項目が分かりやすく表示される。

## CI・品質チェック

基本コマンド:

```powershell
scripts\品質チェック.cmd
```

個別に確認する場合:

```powershell
python -m pytest
python -m ruff check src tests
```

過去の品質状況:

- ruff成功
- pytest成功
- カバレッジ87.78%
- pip-auditはProxyErrorで完了できなかった履歴あり

## 新スレッドへの最初の依頼文例

新しいスレッドでは、次のように依頼すると始めやすい。

```text
このプロジェクトの `引き継ぎ書_新バージョン実装_2026-06-25.md` と `Agent.md` を読んでください。
SAPOSEN Reception Analyzer の新バージョン実装に入ります。
まず Sprint 1 として、GYOMA-AI、ChatSupport、電話CRMの3種類のCSVを読み込む土台を実装してください。
実データは `input_sample_data`、検証用の架空データは `input_sample_data/pipeline_mock_data` にあります。
列順変更に強い列マッピング、UTF-8/CP932対応、不足項目の日本語エラー表示まで入れてください。
```

## 未確定・後日共有予定

- ChatSupportログのI:P列に入る予定の応対コード仕様
- 電話CRMに本番後入力される `電話引継番号` の正式列名
- 電話受付範疇とチャット受付範疇を分ける業務ルール
- 既存GYOMA-AI FAQの有無を判定するためのFAQマスタ
- 再問い合わせ判定に使う期間や同一問い合わせ判定ルール

## 絶対に間違えないこと

- `受付窓口区分名称=チャットサポート` は、ChatSupport経由の電話対応という意味ではない。
- 現時点の実電話CRM履歴だけでは、ChatSupportから電話へ流れたことは確定できない。
- 確定扱いできるのは、将来 `電話引継番号` が電話CRM側に入り、ChatSupport側の `引継番号` と突合できるようになってから。
- 今は、実データでは電話接続を「候補」または「将来接続」として扱う。
- 架空データでは、将来仕様の検証用として `電話引継番号` を入れている。
